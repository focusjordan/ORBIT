[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "orbit-watermark"
version = "2.0.0"
description = "High-robustness neural audio watermarking using Meta FAIR AudioSeal & Resemble AI PERTH fallback"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "Apache-2.0" }
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Topic :: Multimedia :: Sound/Audio :: Analysis"
]
dependencies = [
    "torch>=2.0.0",
    "audioseal>=0.2.0",
    "resemble-perth>=1.0.1",
    "blake3>=0.4.1",
    "librosa>=0.10.0",
    "soundfile>=0.12.0",
    "numpy>=1.20.0"
]

[tool.setuptools]
packages = ["orbit_watermark"]

[project.scripts]
orbit-watermark = "orbit_watermark.silentcipher_watermark:main"
