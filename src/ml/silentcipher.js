/**
 * ORBIT SilentCipher (Deprecated) -> AudioSeal Forwarder
 * 
 * Re-exports AudioSeal engine for backward compatibility.
 * 
 * @deprecated Use `src/engines/audioseal.js` or `src/engines/watermark-unified.js` directly.
 */

module.exports = require('../engines/audioseal');
